{
  "name": "mister-retroarch-save-sync",
  "version": "1.0.0",
  "description": "Watches MiSTer and RetroArch save directories with inotify and keeps saves converted and unified between them. Conversion logic from save-file-converter (GPL-3.0).",
  "license": "GPL-3.0-or-later",
  "private": true,
  "scripts": {
    "build": "esbuild src/vendor-entry.mjs --bundle --platform=node --format=cjs --outfile=dist/converter.cjs",
    "start": "node src/daemon.js"
  },
  "dependencies": {
    "create-hash": "^1.2.0",
    "encoding-japanese": "^2.0.0",
    "lodash-es": "^4.17.21",
    "pako": "^2.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  }
}
